/* DO NOT EDIT OR YOU KNOW WHAT YOU ARE DOING*/
/* DN11 Bird Configure */
/* Examples */
/* Updated At 20250624 */

filter ebgp_import_exmaple {
    check_rpki();
    update_cross();
    accept;
}

filter ebgp_export_example {
    if source !~ [ RTS_STATIC, RTS_BGP ] then reject;
    # if source !~ [ RTS_OSPF, RTS_STATIC, RTS_OSPF_EXT2, RTS_BGP ] then reject;
    add_server_location();
    accept;
}

filter ibgp_import_exmaple {
    accept;
}

filter ibgp_export_example {
    accept;
}

filter bgp_export_local_example {
    if source != RTS_BGP then reject;
    set_bgp_pref();
    accept;
}
