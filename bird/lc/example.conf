/* DO NOT EDIT OR YOU KNOW WHAT YOU ARE DOING*/
/* DN11 Bird Configure */
/* Examples */
/* Updated At 20250624 */

filter dn11_ebgp_import_exmaple {
    # dn11_check_rpki();
    dn11_update_cross();
    accept;
}

filter dn11_ebgp_export_example {
    if source !~ [ RTS_STATIC, RTS_BGP ] then reject;
    # if source !~ [ RTS_OSPF, RTS_STATIC, RTS_OSPF_EXT2, RTS_BGP ] then reject;
    dn11_add_server_location();
    accept;
}

filter dn11_ibgp_import_exmaple {
    accept;
}

filter dn11_ibgp_export_example {
    accept;
}

filter dn11_bgp_export_local_example {
    if source != RTS_BGP then reject;
    dn11_set_bgp_pref();
    accept;
}
